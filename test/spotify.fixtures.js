/*
CREATE TABLE spotify_users (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    spotify_user TEXT NOT NULL
);
*/
function makeSpotifyArray_spotifyUsers() {
  return [
    {
      id: 1,
      spotify_user: "Dunder Mifflin",
    },
  ];
}

/*
CREATE TABLE tracks (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    spotify_user TEXT NOT NULL,
    track_name TEXT NOT NULL,
    track_id TEXT NOT NULL,
    track_href TEXT NOT NULL,
    track_uri TEXT NOT NULL,
    external_url TEXT NOT NULL,
    artist TEXT NOT NULL,
    album TEXT NOT NULL,
    release_date DATE
);
*/
function makeSpotifyArray_tracks() {
  return [
    {
      id: 1,
      spotify_user: "Dunder Mifflin",
      track_name: "Best of You",
      track_id: "5FZxsHWIvUsmSK1IAvm2pp",
      track_href: "https://api.spotify.com/v1/tracks/5FZxsHWIvUsmSK1IAvm2pp",
      track_uri: "spotify:track:5FZxsHWIvUsmSK1IAvm2pp",
      external_url: "https://open.spotify.com/track/5FZxsHWIvUsmSK1IAvm2pp",
      artist: "Foo Fighters",
      album: "In Your Honor",
      release_date: null,
    },
  ];
}

/*
CREATE TABLE playlists (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    spotify_user TEXT NOT NULL,
    playlist_name TEXT NOT NULL,
    playlist_id TEXT NOT NULL,
    playlist_href TEXT NOT NULL,
    playlist_uri TEXT NOT NULL,
    external_url TEXT NOT NULL,
    added_at DATE NOT NULL,
    total_tracks INTEGER NOT NULL
);
*/
function makeSpotifyArray_playlists() {
  return [
    {
      id: 1,
      spotify_user: "Dunder Mifflin",
      playlist_name: "Foo Fighters",
      playlist_id: "58OeUrx5150jMwaN8O8PrA",
      playlist_href:
        "https://api.spotify.com/v1/playlists/58OeUrx5150jMwaN8O8PrA",
      playlist_uri: "spotify:playlist:58OeUrx5150jMwaN8O8PrA",
      external_url: "https://open.spotify.com/playlist/58OeUrx5150jMwaN8O8PrA",
      added_at: "2021-04-18",
      total_tracks: "6",
    },
  ];
}

/*
CREATE TABLE playlist_tracks (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    track_id TEXT,
    playlist_id TEXT,
    spotify_user TEXT,
    added_at DATE NOT NULL
);
*/
function makeSpotifyArray_playlistTracks() {
  return [
    {
      id: 1,
      spotify_user: "Dunder Mifflin",
      track_id: "5FZxsHWIvUsmSK1IAvm2pp",
      playlist_id: "58OeUrx5150jMwaN8O8PrA",
      added_at: "2021-04-18",
    },
  ];
}

/*
CREATE TABLE listening_history (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    spotify_user TEXT NOT NULL,
    played_at DATE NOT NULL,
    track_name TEXT NOT NULL,
    track_id TEXT NOT NULL,
    track_href TEXT NOT NULL,
    track_uri TEXT NOT NULL,
    track_url TEXT NOT NULL,
    artist TEXT NOT NULL,
    album TEXT NOT NULL
)
*/
function makeSpotifyArray_listeningHistory() {
  return [];
}

/*
CREATE TABLE audio_analysis (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    spotify_user TEXT NOT NULL,
    track_id TEXT NOT NULL,
    analysis_url TEXT NOT NULL,
    acousticness NUMERIC (5,3),
    danceability NUMERIC (5,3),
    duration_ms INTEGER,
    energy NUMERIC (5,3),
    explictness TEXT,
    instrumentalness NUMERIC (8,6),
    track_key INTEGER,
    liveness NUMERIC (5,3),
    loudness NUMERIC (5,3),
    mode INTEGER,
    popularity INTEGER,
    release_year NUMERIC (4,0),
    speechiness NUMERIC (5,3),
    tempo NUMERIC (7,3),
    time_signature INTEGER,
    valence NUMERIC (5,3)
);
*/
function makeSpotifyArray_audioAnalysis() {
  return [];
}

module.exports = {
  makeSpotifyArray_spotifyUsers,
  makeSpotifyArray_tracks,
  makeSpotifyArray_playlists,
  makeSpotifyArray_playlistTracks,
  makeSpotifyArray_listeningHistory,
  makeSpotifyArray_audioAnalysis,
};
